/**
 * Default neighborhood radius for registry area insights (scheduling, driller job,
 * optimization form). User can still widen in Optimization up to 25 mi.
 */
export const DEFAULT_AREA_RADIUS_MILES = 2;
