# Session start — Driller Hub (well-driller-dash-board)

**Goal:** Stable pre-departure hub — paste job → map + area insights work in prod without mystery 500s.

**Mode:** shared language → diagnose → small MVP tickets. Not the full multi-month vision.

## Default skills (Hermes)
- `setup-matt-pocock-skills` once if needed (local markdown)
- `grill-with-docs` / `domain-modeling` → `CONTEXT.md` (hub vs DNR viewer)
- `diagnosing-bugs` — API 500s with curl/repro first
- `to-spec` / `to-tickets` — production-stable MVP only
- `improve-codebase-architecture` after APIs green
- `wayfinder` only for big later scope (community notes, etc.)

## First moves this session
1. Read `.context/NORTH-STAR.md` + `OPEN.md`.
2. If APIs failing: **diagnosing-bugs** with a red-capable command before theory.
3. Never “fix” wells by editing CSS marketing site (`css-website-canonical`).

## Done looks like
Core job path green with regression proof — or one clear root-cause ticket in progress.
