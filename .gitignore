data/out/
*.jsonl
*.jsonl.gz
.DS_Store

# DNR statewide export: plain CSV is ~106 MB (over GitHub’s 100 MB cap).
# Commit dnr_wells_full.csv.gz at repo root for a self-contained clone (~22 MB; same export as the viewer repo).
# Optional local .csv from fetch wins at runtime when both exist.
dnr_wells_full.csv
dnr_wells_slim.csv
dnr_wells_slim.csv.gz
.venv/
venv/
__pycache__/
*.pyc

# Next.js (if build artifacts appear at repo root paths)
apps/hub/.next/

# Cursor IDE (local only)
.cursor/
