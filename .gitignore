data/out/
*.jsonl
*.jsonl.gz
.DS_Store

# DNR statewide export: plain CSV is ~106 MB (over GitHub’s 100 MB cap).
# Commit dnr_wells_full.csv.gz at repo root for a self-contained clone (~22 MB; same export as the viewer repo).
# Optional local .csv from fetch wins at runtime when both exist.
dnr_wells_full.csv
dnr_wells_slim.csv
dnr_wells_slim.csv.gz
.venv/
venv/
__pycache__/
*.pyc

# Next.js build artifacts
.next/
apps/hub/.next/

# Dependencies
node_modules/
apps/hub/node_modules/

# Vercel + local env
.vercel
.env*.local

# Cursor IDE (local only)
.cursor/
.env*
data/offline_statewide_extract/

# DNR HTML scrape sidecars (large, local only)
data/dnr_html_scrape/
logs/

# Split chunk sidecars (regenerated by scripts/split_chunks.py from full chunks)
apps/hub/public/well-viewer/dnr_wells_base_chunk_*.csv.gz
apps/hub/public/well-viewer/dnr_wells_litho_chunk_*.csv.gz

# Precomputed area grid (regenerated by scripts/precompute_area_grid.py)
apps/hub/public/well-viewer/area_grid.json.gz
